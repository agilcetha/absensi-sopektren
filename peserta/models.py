from django.db import models
from administrator.models import *

# Create your models here.

# class Absensi(models.Model):
    # nama_peserta = models.ForeignKey(Peserta, verbose_name=('Nama Peserta'), on_delete=models.CASCADE)
    # jadwal_absensi = models.ForeignKey("app.Model", verbose_name=("jadwal"), on_delete=models.CASCADE)